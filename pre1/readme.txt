Note:
old_mappings contains the first revision of the mappings (too lazy to start removing them so i'll just relocate them.

The new mappings are even more MCP-like (including the package re-structuring).